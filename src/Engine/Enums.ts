export enum MouseState {
  CLICK = 'click',
  PRESSED = 'mousedown',
  RELEASED = 'mouseup',
  OVER = 'mouseover',
  OUT = 'mouseout',
  WHEEL = 'wheel',
  DRAG = 'drag',
}

export enum KeyState {
  PRESSED = 'keydown',
  FOCUS = 'focus',
}
